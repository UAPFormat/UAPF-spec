{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.uapf.org/uapf-mcp-tools.schema.json",
  "title": "UAPF MCP Tools",
  "description": "Defines MCP tool descriptors used by agents within UAPF packages.",
  "type": "object",
  "properties": {
    "tools": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Unique identifier for the MCP tool."
          },
          "name": {
            "type": "string",
            "description": "Human readable MCP tool name."
          },
          "description": {
            "type": "string",
            "description": "Purpose of the tool and when to invoke it."
          },
          "inputSchema": {
            "type": "object",
            "description": "JSON Schema describing the tool input payload.",
            "additionalProperties": true
          },
          "outputSchema": {
            "type": "object",
            "description": "JSON Schema describing the tool output payload.",
            "additionalProperties": true
          },
          "endpoint": {
            "type": "string",
            "description": "Endpoint or handler reference for the tool."
          },
          "metadata": {
            "type": "object",
            "description": "Additional tool metadata including safety guardrails.",
            "additionalProperties": true
          }
        },
        "required": [
          "id",
          "name"
        ],
        "additionalProperties": false
      }
    }
  },
  "required": [
    "tools"
  ],
  "additionalProperties": false
}
